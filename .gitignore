__pycache__/
*.py[cod]
.pytest_cache/
.mypy_cache/
.venv/
venv/
.env
.vercel/
data/snapshots/latest-scan.json
data/snapshots/discovery-scan.json
# working files the daily pipeline regenerates inside the CI run
data/snapshots/daily-models.json
data/snapshots/daily-providers.json
# dot-prefixed snapshots are throwaway probe captures, same as .tmp-*
data/snapshots/.*.json
data/candidates.json
data/coverage.json

.vercel
.env*

# local browser/test session artifacts
.playwright-mcp/
gui-test-screenshots/
logs/*.png
logs/*.json

# local manifest used to remove previously generated SEO pages
.seo-pages-manifest.json

# local agent workspace data (project memory, session state) — not for the public repo
.workbuddy-ai/

# throwaway reproduction/scratch files and directories
.tmp-*

# stray UI screenshots captured during local verification
skills-dialog-*.png
skills-grid.png
